/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.8.1/userguide/building_java_projects.html
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
}

project.description = "Java Project Gradle"
project.version = 1.0
project.ext.customProp = 123

project.repositories ({
    println project.name
    println project.description
    println project.version
    println project.path
    println project.projectDir
    println project.buildDir
    println project.customProp
    println project.property("customProp")
    println 'repositories'
    // Use JCenter for resolving dependencies.
    jcenter()
})

project.dependencies ({
    println 'dependencies'
    // Use JUnit test framework.
    testImplementation 'junit:junit:4.13'

    // This dependency is used by the application.
    implementation 'com.google.guava:guava:29.0-jre'
})

project.application ({
    println 'application'
    // Define the main class for the application.
    mainClass = 'dima.gradle.App'
})
